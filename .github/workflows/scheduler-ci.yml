name: Scheduler CI

on:
  pull_request:
    paths:
      - 'scheduler/**'
      - '.github/workflows/scheduler-ci.yml'
  push:
    branches:
      - 'feature/mes-integration-hardening'
    paths:
      - 'scheduler/**'
      - '.github/workflows/scheduler-ci.yml'

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scheduler
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: scheduler/package.json

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
