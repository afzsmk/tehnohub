name: Deploy Tehnohub to GitHub Pages

on:
  push:
    branches: ['main']

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 1. Устанавливаем библиотеки, запускаем тесты и компилируем workforce
      - name: Test & Build Workforce
        working-directory: ./workforce
        run: |
          npm install
          npm test
          npm run build

      # 2. Формируем сайт (главный index.html + скомпилированный workforce)
      - name: Prepare site directory
        run: |
          mkdir -p _site
          find . -maxdepth 1 -not -name '.' -not -name '_site' -not -name '.git' -exec cp -r {} _site/ \;
          rm -rf _site/workforce
          cp -r workforce/dist _site/workforce

      # 3. Публикуем на GitHub Pages
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
